LLM(대규모 언어 모델) 서빙 환경에서 성능 평가의 가장 중요한 두 축은 **지연 시간(Latency)**과 **처리량(Throughput)**. 
사용자 경험을 좌우하는 Latency와 시스템 자원 효율성을 나타내는 Throughput 관점에서 지표를 정리함.

---

## 1. 지연 시간 (Latency) 관점의 지표
Latency는 **단일 사용자**가 서비스를 이용할 때 체감하는 속도와 응답성.

*   **TTFT (Time To First Token)**
    *   **정의:** 사용자가 프롬프트를 전송한 후, 모델로부터 **첫 번째 출력 토큰을 받기까지 걸리는 시간**.
    *   **특징:** 사용자가 '모델이 내 질문을 이해하고 답변을 시작했구나'라고 느끼는 체감 대기 시간에 직결됨. 프롬프트 길이를 분석하는 Prefill 단계의 연산량에 크게 영향을 받음.
    *   **중요성:** 스트리밍(Streaming) 방식으로 답변을 보여주는 챗봇 환경에서 가장 중요한 사용자 경험(UX) 지표.

*   **TPOT (Time Per Output Token)**
    *   **정의:** 첫 번째 토큰이 생성된 이후, **이어지는 토큰들이 하나씩 생성되는 데 걸리는 평균 시간** (보통 ms/token으로 표기).
    *   **특징:** 모델이 답변을 써 내려가는 텍스트 생성 속도(Decoding 단계)를 나타냄.
    *   **중요성:** 사람의 평균 읽기 속도(보통 초당 5~8단어)보다 빠르게 생성되어야 답답함 없이 쾌적한 경험 제공 가능.

*   **E2E Latency (End-to-End Latency)**
    *   **정의:** 요청을 보낸 시점부터 **전체 응답이 완전히 끝날 때까지 걸리는 총 시간**.
    *   **수식:** `E2E Latency ≈ TTFT + (TPOT * (생성된 총 토큰 수 - 1))`
    *   **중요성:** 스트리밍 없이 전체 답변을 한 번에 받아야 하는 API 연동 작업, 문서 요약, 번역 등 비동기 배치(Batch) 작업에서 가장 중요한 지표.

---

## 2. 처리량 (Throughput) 관점의 지표
Throughput은 **서버 시스템**이 특정 시간 동안 얼마나 많은 작업을 처리해 낼 수 있는지를 나타내는 인프라 효율성 지표.

*   **RPS (Requests Per Second)**
    *   **정의:** 서버가 **1초당 처리할 수 있는 전체 사용자 요청(Request)의 수**.
    *   **중요성:** 시스템이 동시에 감당할 수 있는 동시 접속 규모 파악에 사용.

*   **RPM (Requests Per Minute)**
    *   **정의:** 서버가 **1분당 처리할 수 있는 사용자 요청의 수**. RPS에 60을 곱한 값과 유사함.
    *   **중요성:** 상용 LLM API(OpenAI, Anthropic 등)에서 과금 체계를 나누거나, API 호출 제한(Rate Limit) 설정 시 주로 기준이 되는 지표.

*   **TPS (Tokens Per Second)**
    *   **정의:** 서버가 **1초당 생성해내는 총 토큰의 수**. 단일 사용자가 아닌, 서버에 접속한 *모든 사용자의 요청을 합쳐서* 계산.
    *   **중요성:** LLM 서빙 엔진의 **실질적인 컴퓨팅 처리 능력을 보여주는 가장 핵심적인 지표**. 하드웨어(GPU) 활용도를 극대화할수록 증가.

---

> Latency와 Throughput의 트레이드오프(Trade-off)**
> LLM 서빙에서는 한 번에 여러 요청을 묶어서 처리하는 **배치 크기(Batch Size)**를 늘리면 GPU 활용도가 높아져 **Throughput(TPS, RPS)은 증가**. 
> 하지만 각 사용자는 다른 사람의 요청이 함께 묶여 처리될 때까지 기다려야 하므로 **Latency(TTFT, E2E)는 악화**. 
> 따라서 서빙 환경에서는 목표로 하는 사용자 체감 속도(TTFT/TPOT)의 마지노선을 지키면서 TPS를 최대로 끌어올리는 것이 최적화의 핵심.
